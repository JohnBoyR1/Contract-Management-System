import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class ChatApi {
  /* NOTE: This service is currently empty.
     It was intended to handle backend API calls for messaging,
     but no backend/RDS/cloud integration was completed.
     The app is using local-only storage instead (ChatStore). */
}
